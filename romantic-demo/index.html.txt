<!DOCTYPE html>
<html>
<head>
    <title>Romantic Surprise ❤️</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #ff758c, #ff7eb3);
            text-align: center;
            color: white;
            overflow-x: hidden;
        }

        .container {
            padding: 80px 20px;
        }

        h1 {
            font-size: 45px;
            animation: fadeIn 2s ease-in-out;
        }

        p {
            font-size: 20px;
            max-width: 600px;
            margin: 20px auto;
            line-height: 1.6;
        }

        button {
            margin-top: 25px;
            padding: 12px 30px;
            font-size: 16px;
            border: none;
            border-radius: 30px;
            background: white;
            color: #ff4e78;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            background: #ffe3ec;
        }

        .gallery {
            margin-top: 50px;
        }

        .gallery img {
            width: 200px;
            border-radius: 15px;
            margin: 10px;
            transition: transform 0.3s;
        }

        .gallery img:hover {
            transform: scale(1.1);
        }

        #surprise {
            font-size: 22px;
            margin-top: 20px;
            font-weight: bold;
        }

        /* Floating Hearts */
        .heart {
            position: fixed;
            bottom: -10px;
            font-size: 20px;
            animation: float 5s linear infinite;
        }

        @keyframes float {
            0% { transform: translateY(0); opacity: 1; }
            100% { transform: translateY(-100vh); opacity: 0; }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
    </style>
</head>

<body>

<div class="container">
    <h1>Happy Birthday My Love ❤️</h1>

    <p>
        You are the most beautiful part of my life.  
        This little surprise is just a reminder that you mean everything to me.  
        Thank you for being my happiness.
    </p>

    <button onclick="showSurprise()">Click for Surprise 🎁</button>

    <p id="surprise"></p>

    <div class="gallery">
        <h2>Our Memories 💕</h2>
        <img src="https://via.placeholder.com/200" alt="Memory 1">
        <img src="https://via.placeholder.com/200" alt="Memory 2">
        <img src="https://via.placeholder.com/200" alt="Memory 3">
    </div>

    <button onclick="playMusic()">Play Romantic Music 🎵</button>

    <audio id="bgMusic">
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
    </audio>
</div>

<script>
function showSurprise() {
    document.getElementById("surprise").innerHTML = "I Love You Forever and Always ❤️";
}

// Floating hearts generator
function createHeart() {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.fontSize = Math.random() * 20 + 15 + "px";
    document.body.appendChild(heart);

    setTimeout(() => {
        heart.remove();
    }, 5000);
}

setInterval(createHeart, 500);

function playMusic() {
    document.getElementById("bgMusic").play();
}
</script>

</body>
</html>